# 萌宠小屋 v8 · 剩余待办落地进度

按用户指令「按 A→B→C→D 来」顺序推进，A/B/C 已于前序会话完成，本次会话完成 **D 项**。

## D 项：学科内容扩充（可编辑题库框架 + 占位题库）✅
落地点：`v8.html`（单文件应用，约 12600 行）

### 1. 数据模型（存本机 localStorage，键 `mp_qbank_v1`）
- 七大学科：`识字 / 拼音 / 口算 / 英语 / 古诗 / 科普 / 逻辑`
- 每题结构：`{ id, q, a, opts?, type('fill'|'choice'|'read'), hint?, reviewed }`
- `reviewed:false` 标记 = AI 占位内容，需真人教研审核后勾「已审」再作准。

### 2. 家长设置「📚 学科题库（可编辑 · 占位待审）」区
- 学科分页 tab（显示各科题数），题目列表（🟡 待审标记 / ✔ 已审 / 改 / 删）
- 添加题目：学科 → 题型(填空/选择/跟读) → 题干 → 选项或答案 → 提示，链式自建弹窗
- 编辑 / 删除 / 切换已审 / 一键「恢复占位题库」

### 3. 孩子端「题库闯关」屏 `sc-qbank`
- 学习乐园网格新增「📚 题库闯关」入口卡片
- 读取家长题库，按学科抽题；填空输入 / 选择题按钮 / 跟读题（语音+「我会读啦」）
- 答题即时反馈，答对 +1 ⭐（走统一积分流水 `addEarn`），答错显示正确答案

### 4. 对外 API（供后续游戏/学习模块读取）
`window.getBank(subject)` / `drawFromBank(subject,n)` / `bankAdd(...)` 等已导出。

## 测试（jsdom，0 运行时错误）
- `_test_bank.js`：D 项 **16 项全 PASS**（默认占位覆盖 7 科 / 待审标记 / 增题存本机往返 / 已审切换 / 闯关渲染 / 答对答错判定）。
- 回归：`_test_phonics_game` / `_test_states` / `_test_bgm` / `_smoke_v8` 全部 ALL PASS。

## 关键排障记录
- `renderQBank` 内用 `Array.filter` 回调读取 `CONTENT_BANK.subjects[s]` 时出现闭包绑定诡异现象（字面量能取到、迭代变量取不到），改用显式 `for` 循环后正常。等价写法，更稳。

## 待用户后续（真人教研）
当前题库为 AI 占位示例，答案正确性需由你/教研审核：进入「家长设置 → 学科题库」，逐题核对后勾选「已审」，或替换/补充为正式题目。
