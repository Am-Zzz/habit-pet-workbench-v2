# 绘本乐园 · 升级诊断与规划

> 视角：交互架构师 + 儿童阅读产品 + 育儿老师  
> 目标：把「绘本」从"纯文字翻页书"升级为"混合式阅读教室"——文本页 + 媒体页（内嵌网上 PPT/视频大图）+ 互动页，并打通网盘，让线上优质内容直接进阅读流。

---

## 一、当前问题诊断（基于代码逐行核对）

### 1. 英文绘本是空壳（伪功能）

`renderBookList` 里 `bookLang==='en'` 直接渲染 `mcEmpty('📚','英文绘本整理中')`，顶部却一直挂着"英文绘本"Tab。孩子一点进去是空页，属于"有入口没内容"的半成品，容易破坏信任感。

### 2. 顶部「+ 加入书架」按钮语义错误

`onclick` 直接 `switchBookNav('shelf',...)`——它只是**跳到书架视图**，并没有"把当前某本书加入书架"的逻辑。真正的加书架是在每本书卡片上的 `+` 角标。这个按钮名不副实，应删掉或改成"管理书架/去书架"。

### 3. 阅读器只支持文本型页，无法内嵌 PPT / 视频大图（核心诉求未满足）

阅读器 4 个 Tab（封面/正文/诗歌/互动）全部基于 `pages[].chars`（汉字数组）+ emoji 装饰插图。**没有任何一页能承载外部富媒体**：既不能在整页铺一张大图，也不能嵌 PPT、放视频。这正是你这次想加的能力，目前为零。

### 4. 网盘「绘本」与阅读器是割裂的两套

网盘 `book` 板块导入的链接，只在"我的绘本"面板里以卡片展示，点开是 `netOpen` **外部新窗口打开**，进不了绘本的阅读流。两套系统没有打通，导致"网上的 PPT/视频绘本"无法被真正"内嵌阅读"。

### 5. 插图自定义只能"换小图"，且是装饰性的

`openReaderImgPicker` 能换封面/正文插图（链接/上传/AI），但只是替换角落那块 emoji 插图区，不是"整页大图/PPT/视频"。而且链接要手动粘，没和网盘打通。

### 6. 互动页只有"单题选择 + 简笔画"

`quiz-screen` 从 `curBook.quiz` 取**一题**做选择，加一个画板。缺少：逐字点读、跟读录音反馈、按页配套互动、PPT 后接讨论题等更丰富的学习闭环。

### 7. 正文只能整页朗读，无"逐字点读"

`speakBookPage` 是整页 TTS。虽然字符带拼音，但"点哪个字读哪个字"没做，对幼小衔接识字阶段不友好。

### 8. 返回链路依赖 `goBack()`，从非书库入口进入易跳错

书库顶部 `←` 用 `goBack()`（依赖历史栈）；阅读器内 `← 返回书库` 用 `closeReader()` 隐藏浮层。两者不是同一套逻辑，从游戏中心/英语乐园点进来再层层返回时，可能回错屏（与之前地图返回问题同源）。

### 9. 缺分级阅读推荐，未与其它学习板块联动

虽然有 AA/A/B 分级和年龄分类，但只是"筛选"，没有"读完这本→推荐下一本/相关自然拼读练习"的衔接，也没和英语乐园·自然拼读形成学习闭环。

---

## 二、升级总目标

| 维度   | 现在             | 升级后                           |
| ---- | -------------- | ----------------------------- |
| 内容形态 | 纯文字 + emoji 插画 | 文本页 + **媒体页（PPT/视频大图）** + 互动页 |
| 外部内容 | 网盘链接仅外部打开      | 网盘 PPT/视频**一键插入阅读流**          |
| 朗读   | 整页 TTS         | 整页 TTS + **逐字点读** + 跟读反馈      |
| 互动   | 单题 + 画板        | 每页可选配（选择/拖拽/点选/录音）            |
| 英文绘本 | 空壳             | 补真实内容 + 与拼读联动                 |
| 家长管控 | 无媒体审核          | 外链白名单 + 是否允许联网                |

---

## 三、核心方案：媒体型书页（media page）

### 3.1 数据模型扩展（不改旧数据，向后兼容）

在现有 `book.pages[]` 上新增页型，旧 `text` 页完全不受影响：

```
page = {
  type: 'text',                       // 现有：封面/正文/诗歌
  // 新增：
  type: 'media',
  carrier: 'embed' | 'video',         // embed=内嵌网页(PPT类) / video=视频
  src: 'https://...',                 // PPT 发布链接 或 视频直链
  title: '第3页：动物世界',
  poster: 'https://...封面图',         // 视频封面（可选）
  note: '看完想一想：你最喜欢哪种动物？'  // 页尾引导语
}
```

### 3.2 阅读器渲染分支

- 翻到 `media` 页时，**整屏切换为"大画面模式"**：隐藏文字区，给儿童一个大画面（PPT 铺满 / 视频居中播放）。
- `embed` 页：`<iframe sandbox="allow-scripts allow-same-origin" >` + 顶部「全屏 / 退出全屏」按钮；外链走白名单校验。
- `video` 页：`<video controls>`；若为网盘分享链接，复用现有 `/api/netdisk/stream` 解析能力内联播放。

### 3.3 把网盘与绘本打通（你这次的核心想法）

- 「我的绘本」面板里的网盘 `book` 链接 → 新增「插入到某绘本第 N 页」→ 自动生成 `media` 页。
- 阅读器内新增「＋ 插入媒体页」入口，可贴 PPT/视频链接或选网盘内容。
- 这样"网上 PPT 绘本 / 视频绘本"就真正**内嵌进阅读流**，而不是新开窗口。

---

## 四、交互升级（点读 / 跟读 / 富化互动）

1. **逐字点读**：正文每个汉字包成可点元素，点击即朗读+高亮（复用 `voiceForRole`）。
2. **跟读录音反馈**：复用现有 `toggleBookRec`，加一个"读得怎么样"的轻量反馈（响度/时长 → 小星星），让孩子有成就感。
3. **互动页按页配套**：每页可挂 1 个互动（选择/拖拽配对/点选识字/录音），PPT 媒体页后自动接一个"讨论题"卡片。

---

## 五、家长 / 教师模式

- 媒体页**增删**入口（家长模式可见，孩子端只读）。
- **外链白名单审核**：只允许白名单域名内嵌（腾讯文档/金山文档/Google Slides/优酷/B站 等可配置），未知域名降级为"外部打开"并提示家长。
- 开关：**允许联网 / 仅本地**——护眼、防走神。

---

## 六、实施路线图（分阶段，每阶段可独立交付）

### 阶段 1 · MVP（先把"内嵌 PPT/视频"跑通）⭐ 建议先做

- [ ] `pages[]` 支持 `media` 页型 + 阅读器渲染分支（embed/video 大画面）
- [ ] 网盘 `book` 链接「一键插入为媒体页」
- [ ] 阅读器内「＋ 插入媒体页」+ iframe 白名单 sandbox
- [ ] 修掉诊断 2、4（删除错误按钮 / 打通网盘）
- [ ] 交付：孩子能在一本书里翻到"老师发的 PPT 大图页 / 视频页"

### 阶段 2 · 阅读体验增强

- [ ] 逐字点读 + 跟读录音小星星反馈
- [ ] 互动页富化（拖拽/点选/按页配套）
- [ ] 修掉诊断 8（统一返回逻辑）

### 阶段 3 · 内容与闭环

- [ ] 英文绘本补真实内容（与英语乐园·自然拼读联动）
- [ ] 分级阅读推荐（读完这本→推下一本/相关拼读练习）
- [ ] 家长白名单审核面板 + 联网开关

---

## 七、技术落点（给你心里有数）

- 仍是**单文件 v8.html + localStorage 持久化**，不引入后端。
- 联网内容复用已有 `/api/netdisk/stream` 代理（网盘解析），避免前端直连被跨域/CORS 卡住。
- iframe 用 `sandbox` + 域名白名单，防注入、防孩子误触外跳。
- 媒体页数据存 localStorage（用户自建绘本 / 插入的网盘内容），内置 `BOOK_CONTENT` 保持只读。

---

### 建议下一步

我可以先落地**阶段 1 MVP**：把"媒体页 + 网盘插入 + 阅读器大画面"做出来，给你一个能翻 PPT/视频绘本的真原型。你确认后我直接改 v8.html 并配回归测试。
